import React from 'react';
import {Input} from 'antd';
const Search = () => {
    return (
        <Input.Search placeholder="Search...." styles={{marginBottom: 10}}/>
    );
};

export default Search;